import { SettingsSection } from '../SettingsSection';

/** Integrations settings — connect external services (placeholder for now). */
export function IntegrationsPanel() {
  return (
    <SettingsSection title="Integrations">
      <p className="settings-empty">No integrations available yet.</p>
    </SettingsSection>
  );
}
