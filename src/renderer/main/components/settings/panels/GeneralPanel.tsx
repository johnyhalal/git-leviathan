import { SettingsSection } from '../SettingsSection';

/** General settings — placeholder until real options land. */
export function GeneralPanel() {
  return (
    <SettingsSection title="General">
      <p className="settings-empty">More settings coming soon.</p>
    </SettingsSection>
  );
}
